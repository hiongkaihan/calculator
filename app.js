const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a + b;

let displayValue = 0;
//const operate = function(operator, a, b) => operator()